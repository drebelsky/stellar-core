#pragma once
#include "util/config.h"

namespace PopulateOptions
{
constexpr Options options = Options::PRESIZE;
constexpr DataEntriesType type = DataEntriesType::DEFAULT;
constexpr Mode mode = Mode::NORMAL;
}
